systemLog.quiet: true
net.bindIpAll: true
# Test harness only: PCSM-381 E2E arms the failCommand failpoint on the target
# mongos to make moveChunk fail during pre-split.
setParameter:
  enableTestCommands: 1
